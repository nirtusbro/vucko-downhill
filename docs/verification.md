# Verification — 22 September 2026

## Automated checks

- Ghost from passed runs: 65 tests pass, including checks that a failed run never sets a level best, that every level has a distinct layout fingerprint, and that a ghost saved under another fingerprint is ignored on load.
- Ghost from passed runs: 65 tests pass, including checks that a failed run never sets a level best, that every level has a distinct layout fingerprint, and that a ghost saved under another fingerprint is ignored on load.
- Lamps kept on a pass: 64 tests pass, including a check that a passed run keeps all four new pickups, a failed run keeps none, an unfinished run keeps none, and a replay keeps only pickups not already owned.
- No boost: with boost removed, 63 tests pass; the sweep finds every gate and pickup reachable on all twenty levels with no tumbles, every goal passable, and a straight line failing everywhere. The HUD no longer shows a Speed up button, and the how-to page describes steering only.
- Clean passes: after moving Squeeze and Halfway sprint to levels 4 and 5, the sweep still finds every gate and pickup reachable on all twenty levels with no tumbles, every goal passable without boost except the two boost levels, and a straight line failing everywhere; a run that misses a gate can no longer pass, and a tumble deducts 300 points. Browser at 390 × 844: an un-steered level-one run turned the HUD goal line red at the first missed gate, the results read the missed-gate count with “every gate is needed”, and the next-level card stayed hidden; the card itself renders above the results table with the next level’s name, hint, lamp and goal.
- Faster base speed: with every level 15% faster, the sweep still finds every gate and pickup reachable on all twenty levels with no tumbles; the worst gate margin is 0.62 m on Zigzag and 0.92 m on Tight corridor. The boost levels were retuned to a one-second demand with par 2% above cruising so that a perfect cruising run still fails and the careful boosted run passes both.
- Harder ladder: 67 tests pass, including the new rule that consecutive gate openings never overlap sideways unless a rock sits on the line between them, checked on every stretch of every level. The sweep with the path-swerving controller found every gate and pickup reachable on all twenty levels with no tumbles; worst gate margins are 0.97 m on Tight corridor, 1.0 m on Zigzag and 1.34 m on The summit, and above 1.4 m elsewhere. A perfect cruising run scores 16,250 against 16,700 on Speed run and 20,000 against 20,400 on The summit while boosted runs score 17,046 and 20,592; a straight line fails all twenty. Hazard counts now run from 0 on First tracks to 26 on The summit.
- Hand-placed levels: 67 tests passed. The sweep of all twenty authored layouts with the steering controller found every gate and pickup reachable with no tumbles and at least 1.2 m of gate margin, every goal passable without boost except Speed run and The summit, where a perfect cruising run scores 16,300 against 16,700 and 20,200 against 20,400 while the boosted run scores 17,217 and 20,940; a straight line fails all twenty; no stretch carries two features; every hazard is at least 12 m from a gate line; guard trees match the authored lists; and a replay with two of level one’s lamps already owned starts with those two collected, counts “0 / 2”, scores only the other two and keeps the goal unchanged. Browser at 390 × 844: with lamps 1 and 2 saved, the home card lit two of the four slope lamps and read “2 / 100 found”; the run’s HUD read “0 / 2 lamps” with the chairlift, fringe trees and lamp posts rendering beside the course. No console errors.
- Designed levels: 66 tests passed. A sweep of all twenty designs with the steering controller found every gate and pickup reachable with no tumbles and at least 2.0 m of gate margin, every goal passable without boost except Speed run and The summit, where a perfect cruising run scores 16,373 against a 16,700 goal and 18,513 against 18,700 while the boosted run scores 17,310 and 18,877; a straight, un-steered line fails all twenty; no level has more than one nearly straight stretch in a row; and two runs of the same level produce identical presents. The home card at 390 × 844 showed “Level 1 · First tracks” with its hint, and with level 16 unlocked “Level 16 · Speed run — Gates alone will not do it. Boost, and pick up every lamp.”, 22 gates and goal 16,700. No console errors.
- Twenty levels, five lamps each: 64 tests passed across levels, progress, physics, presents, hazards, ghost, pressure, boost, catalogue and finish-time scoring. Checks confirm the twenty layouts are deterministic and distinct, that their five-lamp sets cover all hundred catalogue lamps exactly once from Common to Legendary, that every level always places four pickups, that a relaxed steering controller hits every gate and pickup on all 20 levels for two seeds with no tumble and at least 0.5 m of gate margin while passing every goal, that a straight un-steered line does not pass level 1, that pickups are saved at once and finish lamps only on a pass, and that rocks grow with level.
- Browser check at 390 × 844: the home card read “Level 1 of 20”, “10 gates · Goal 2,900”, with four dark slope lamps under the finish lamp. An un-steered run showed “0 / 4” lamps and “Goal 2,900” in the HUD and finished with 7 of 10 gates and 1,911 points; the results read “Level 1 · not quite”, “Goal 2,900 · 989 short”, kept the finish lamp as a silhouette, offered Try again and hid Next level. With a saved collection of three lamps the home card showed “Satellite halo” in colour, two lit slope lamps, best 3,200 and “Ski level 1 again”; the level map listed 20 groups of five lamps with level 1 at 3 / 5, level 2 highlighted as Next with its goal and level 3 locked. No console errors.
- Earlier single-lamp ladder: 62 tests passed across levels, progress, physics, presents, hazards, ghost, pressure, boost, catalogue and finish-time scoring. New checks build all 100 courses and confirm they are deterministic and pairwise distinct, that speed, gate width, spacing, gate count, hazard chance and goal fraction move monotonically, that a relaxed steering controller hits every gate and pickup on all 100 levels with no tumble and at least 0.5 m of gate margin while passing every goal, that hazards stay 12 m from gate lines with a fork rock beside every pickup from level 5, that presents land clear of hazards, and that progress unlocks only on a pass, caps at level 100, survives reloads and ignores damaged saves.
- Level ladder browser check at 390 × 844: the home screen showed “Level 1 of 100”, the level-one lamp as a silhouette, “8 gates · Goal 1,800” and “Ski level 1”. An un-steered run showed “Goal 1,800” under the score and finished at 22.6 s with 8 of 8 gates and 4,066 points; the results read “Level 1 complete!”, “A new lamp for Ljubica.”, “Goal 1,800 reached”, revealed “Rose mushroom · Common · New!” and offered Next level. Because a straight line hit every gate, early offsets were widened, early gates narrowed and the starting goal raised to 56%; a straight line now scores 1,866 against a 2,000 goal on level 1. After reload the home card read “Level 2 of 100”, the shelf “1 / 100 earned”, and the level map listed 100 cards with one earned (best 4,066), level 2 highlighted as Next and 98 locked. No console errors.
- Earlier gameplay-depth checks (superseded by the ladder): 74 tests passed across physics, difficulty, collections, presents, hazards, ghost, pressure, boost and finish-time scoring. Checks covered a fork rock at every lamp and a guard tree beside Rare and better lamps, hazards at least 12 metres from every gate line, more rocks in harder modes with seeded reproducibility, tumbles on rocks, present landings clear of hazards, bullseye scoring, the ×8 combo cap, the boost turning cost, gate tightening and the slope ramp, ghost sampling, pace deltas and storage validation, plus 100 unique stable designs, distinct seeded picks per run, 8 seeded lamp spots per run spread over every band of stretches with every spot reachable across seeds, lamps held 5 metres wide of the gate line, present drops limited to two to five per run at least 9 seconds apart, all designs obtainable across seeded runs, rarity weighting, undiscovered-design preference, legacy count migration, reload persistence, and a single finish-only time bonus.
- A controller using only normal steering completes the course in every difficulty for six seeds each, hits all 20 gates and collects all 8 lamps for 13,200 gate points plus bullseye, lamp, present and time bonuses, in 27–60 seconds. A 60-seed sweep per mode outside the suite found every gate and lamp reachable, with worst gate margins of 0.71, 0.30 and 0.10 metres in Easy, Classic and Expert. Every mode reaches its increased pace within three seconds.
- TypeScript and production Vite build pass. Production JavaScript is approximately 140 KB gzip, with two local font subsets and home-screen icons.
- `npm audit` reports no known dependency vulnerabilities.

## Browser checks

- Inspected portrait framing at 390 × 844 and 360 × 800 and landscape at 844 × 390 and 568 × 320 in the Chromium-based in-app browser. Verified small landscape result dialogs scroll to all actions.
- Start, how-to-play, pause, resume, restart and finish screens exercised through their actual buttons.
- Drag steering checked visually against skier movement, snow spray and world-space tracks.
- A full browser run finished in 1:29.4 with 17/20 gates and 5,600 points before the birthday additions.
- Earlier birthday version: home screen displays “Happy birthday, Ljubica!”, lamp/table display and small gifts. Four lamp designs now sit in open stretches between gate stations. An earlier full browser run finished in 1:13.0 with 8 gates and 4 lamps, correctly scoring 1,000 points. Replay reset lamps, gates and score to zero. Pause/resume preserved progress.
- Faster version: a Classic browser run finished in 47.3 seconds with 7 gates, 1 lamp and 3 presents. The result correctly displayed 1,650 total points and +600 present points, preserving the existing Classic best of 8,350. Gold landing rings and wrapped gifts rendered on the slope; Expert reached 132 km/h on a straight. Mode selection, independent best labels and zeroed replay counters were checked through actual controls.
- Inspected new home controls at 390 × 844, compact HUD/results at 320 × 568, and landscape home at 568 × 320. Fixed overlapping landscape title/selector and cramped narrow-phone HUD labels. Result dialogs remain scrollable to all actions.
- Keepsake shelf: an Easy run on the 1,000-metre course finished in 42 seconds, collecting 3 lamps. Returning home showed one rose mushroom, one lavender pleated lamp and one blue porcelain lamp. Reloading retained all three and their matching counts. The home shelf uses four compact illustrations, which gain color when collected. Its portrait position accounts for the top safe-area inset.
- Speed control: inspected the lower-left hold button at 390 × 844 and 320 × 568. A pointer drag beginning on the button increased Classic speed above its normal cruising range and released cleanly. Pause hid the control. The home high-score metric displayed the saved Classic 9,950, switched to Expert 0 and restored Classic correctly; it remains visible at 568 × 320. Simultaneous thumb handling is covered by automated pointer-event tests; physical two-thumb feel still needs a phone check.
- Independent source review found no remaining important defects in core or birthday features.
- Gameplay depth: an un-steered Classic browser run at 390 × 844 tumbled on a fork rock beside a lamp with the “A little snow hug!” message, finished in 42.2 seconds with 3 gates and 2,689 points, showed “0 bullseyes · +0 points” on the results, and saved a 423-sample ghost for Classic. The replay showed a translucent Vučko just behind the skier, the pace readout under the timer moving from −0.0 s to −0.6 s in green, presents landing clear of rocks, and no console errors.
- Sparser pickups: an un-steered Classic browser run at 390 × 844 finished in 33.7 seconds with 7 gates, 1 present and 0 lamps, scoring 4,014 with the results reading “0 / 8 lovely lamps” and “+200 points”. The HUD showed “0 / 8” from the start, a lamp rendered at its seeded spot beside the racing line, and the console stayed clean. The production build passes again after replacing a Node-only Buffer call in the silhouette test.
- 100-lamp catalogue: confirmed the previous four designs migrated with counts 15/12/11/11, all 100 slots rendered, missing lamps had solid black silhouettes, and the Legendary filter showed exactly two slots. A completed Easy run displayed 9,150 on-slope points +2,113 time points =11,263, with 15 new discoveries bringing the collection to 19/100. The Collected filter showed the matching 19 named designs; all survived a page reload. Reviewed mobile gallery artwork at 390 × 844. Scene rendering is skipped while the opaque collection page is open.

## Physical-device and deployment checks still to do

The browser viewport checks are not physical iPhone or Android tests. Frame rate, battery use, real touch feel, Safari audio interruptions, notch/safe-area behavior and Add to Home Screen need final checks on real devices. No measured 60-FPS guarantee is made. The game is running locally; no public hosting deployment has been performed. Offline caching is not implemented.
